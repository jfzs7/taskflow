apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: taskflow
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
  clusterIP: None # headless service dla StatefulSet
